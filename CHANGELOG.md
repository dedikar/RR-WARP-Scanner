Формат версий: `x.y.z-rN` (r1..r99, затем `x.y.(z+1)-r1`).
Сборка: `./build.sh` (автоинкремент через `version.txt`) или `./build.sh <версия>`.
Собирается в `build/luci-app-warpscan_<версия>_all.ipk`, бэкапы — в `backup_<timestamp>/`.

## 0.2.0-r8
- Детект «torn down» по образцу WARPSCOUT: в фазе 2 каждый эндпоинт прогоняет
  серию ICMP через туннель (`ping -c5` до 1.1.1.1 через интерфейс wgscan).
  Если хвостовая серия из >= TEAR_RUN(2) эхо пропала (последний seq < лимита) —
  туннель резан DPI на середине потока, эндпоинт помечается `torn`.
- Новые колонки: TUN PING (rtt через туннель) и LOSS (% потерь в той же бурсте).
- Сортировка: workable (не-torn) идут вверх по пингу, torn — в конец; BEST никогда
  не выбирается из torn (фронт + applyBest).
- В UI torn-строки подсвечиваются красным и бейджиком TORN; «Скопировать лучший
  .conf» отключается, если рабочих нет. Формат строки результата стал
  `ip:port NODE LOC rtt tun_rtt tun_loss torn`.
- busybox-фикс: парсинг `seq=` (не `icmp_seq=`), `-i` не используется (интервал 1s).

## 0.2.0-r7
- фикс парсинга TUN PING.

## 0.2.0-r5
- Результаты скана сохраняются персистентно: при старте нового скана старая
  таблица копируется в `/etc/warpscan-last-result.txt` (переживает ребут роутера).
- `scanResult` при отсутствии свежего файла отдаёт сохранённый с флагом
  `saved:true`; при загрузке вкладки предыдущие результаты сразу показываются,
  заголовок «Сохранённый скан от …» / «Скан от …».

## 0.2.0-r4
- Кнопки «Быстрый поиск» и «Полный поиск (долго!)» блокируются на время
  сканирования и разблокируются по завершении или ошибке (повторный запуск
  во время идущего скана стал невозможен на фронте).

## 0.2.0-r3
- Поллинг статуса догоняет при возврате на вкладку: слушатели `focus` /
  `visibilitychange` сбрасывают таймер и сразу перечитывают статус, пока скан
  идёт (в фоновой вкладке setInterval тормозит).

## 0.2.0-r2
- Дату/время скана в шапке результатов: `scanResult` возвращает `updated`
  (mtime файла результата через `stat()`), фронт показывает «Скан от …».

## 0.2.0-r1
- Переход на схему `x.y.z-rN` в `build.sh` + `version.txt`.
- Актуальная линия версий 0.2.0 (сохранены установочные бэкапы).

## 0.1.x … 0.2.0 (ранняя линия)
- Двухфазный скан: handshake-sweep (перебор хостов) + честный ICMP-пинг с
  замером метаданных (порты 2408/1700/4500/500).
- Параллельный скан воркерами wgscan0..N-1 (`-j`, 1..6) — ускорение ~2.3х
  на 40 хостах.
- Монотонный прогресс: фаза 1 → до 80%, фаза 2 с живым счётчиком `scanned2.cnt`
  → 80..99%, done → 100%; память `/tmp/wscan/percent`, тексты фаз.
- Чистка накопления файлов воркеров (hosts.*.txt / alive.*.txt) при старте.
- `renderTable` + `confPreBox`: «Показать .conf» раскрывается/сворачивается в
  своей строке (в т.ч. последней), ранее вставлялся в конец блока.
- Копирование отдельных конфигов: «Скопировать .conf» у каждой строки +
  «Скопировать лучший .conf» (navigator.clipboard с фолбэком на
  выделение+execCommand).
- «Скачать всё .txt» → `warpscan-configs.txt`: полные конфиги для каждого
  эндпоинта с заголовком (`# ip:port node country ping ms`) и датой скана.
- Показ фаз («Фаза 1: перебор N/M, активно K», «Фаза 2: замеры, обработано N/M»).
- Кнопки переименованы: «Быстрый поиск», «Полный поиск (долго!)».

## 0.1.0 (первый рабочий)
- Базовое LuCI-приложение: форма сканирования, прогресс-бар, таблица результатов
- Регистрация WARP-аккаунта через `wregister.sh`, импорт лучшего эндпоинта.
- Обфускация (Jc/Jmin/Jmax/H/Fϕ/I) только в выдаваемых `.conf` (на сканирующем
  интерфейсе убрана — kmod-amneziawg виснул при обфускации).